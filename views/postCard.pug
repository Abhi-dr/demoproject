each post in posts
    .post-card
        h2=post.title 
        h3="By "+post.author
        p=post.content 
        form(method="post" action="/admin/moderate")
            button.adminBtn(value="approve") Approve
            button.adminBtn(value="decline") Decline